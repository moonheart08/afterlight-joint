<screen:EditorScreen xmlns:screen="clr-namespace:Content.ALED.Screen"
                     xmlns="clr-namespace:Content.Client._AL.UI.Widgets;assembly=Content.Client"
                     xmlns:editorUI="clr-namespace:Content.ALED.EditorUI"
                     HorizontalExpand="True"
                     VerticalExpand="True">
    <VBorderedPanel Name="Container">
        <!-- Top bar -->
        <HBorderedPanel Margin="0">
            <Button Text="File"/>
            <Button Text="Edit"/>
        </HBorderedPanel>
        <HGrowStack Margin="0" VerticalExpand="True">
            <VBorderedPanel Margin="0 4 0 0">
                <RichText Text="Side panel would go here"/>
            </VBorderedPanel>
            <editorUI:NodeBoard/>
        </HGrowStack>
    </VBorderedPanel>
</screen:EditorScreen>
